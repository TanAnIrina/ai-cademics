# The frontend talks to the backend through a same-origin "/api" path.
# In development, Vite proxies "/api" to the address below.
# In production, nginx proxies "/api" to the backend container.
VITE_DEV_API_TARGET=http://localhost:8000
